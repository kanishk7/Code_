#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char s1[10], s2[10];
    printf("Enter string 1: ");
    fgets(s1,10,stdin);
    fflush(stdin);
    fgets(s2,10,stdin);
    fflush(stdin);
    printf("s1 = %s",s1);
    printf("s2 = %s",s2);    

	int E[20][20];






}
