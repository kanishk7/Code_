#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/*
Takes input from the user(an integer value n). The output will be the next palindrome that is bigger than
the given integer n.
*/


int main()
{
    int a,b,c,i;
    char n[12];
    printf("Enter the number:");
    scanf("%s",s);

    int a[12];
    for(i = 0; i<12; i++)
    {
        a[i] = -1;
    }








    return 0;
}

